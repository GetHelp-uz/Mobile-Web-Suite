kind = "web"
previewPath = "/"
title = "ToolRent - Web Dashboard"
version = "1.0.0"
id = "artifacts/web-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 5000
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/web-app run dev"

[services.env]
PORT = "5000"
BASE_PATH = "/"

[services.production]
build = ["pnpm", "--filter", "@workspace/web-app", "run", "build"]
publicDir = "artifacts/web-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
